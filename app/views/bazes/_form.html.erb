<%= form_with(model: baze, local: true) do |form| %>
  <% if baze.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(baze.errors.count, "error") %> prohibited this baze from being saved:</h2>

      <ul>
      <% baze.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
