<%= form_with(model: child, local: true) do |form| %>
  <% if child.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(child.errors.count, "error") %> prohibited this child from being saved:</h2>

      <ul>
      <% child.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :person_id %>
    <%= collection_select(:child, :person_id, Person.all, :id, :bd, {:prompt => true, :multiple=>true }, id:"simple-example" )%>

  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :dob %>
    <%= form.text_field :dob, :class => 'dobchild', data: { date_format: 'dd/mm/yyyy' }, placeholder: "dob child" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
